importante revisar la imagen

Instalacion de posgradeSQL
Instalacion de pgvector
Un archivo schema.sql > CREATE EXTENSION IF NOT EXISTS vector;

Si se usa alembic es importante declarar 
import pgvector.sqlalchemy > script.py.mako